export type LunchMenuType = {
  id: string;
  date: string;
  title: string;
  image: string;
  description: string;
};

export type fileType = {
  lastModified: number;
  name: string;
  size: number;
  type: string;
  lastModifiedDate: Date;
};
